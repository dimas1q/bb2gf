# ====== Bitbucket Server/DC (обязательно заполнить) ======
# Если запускаете по ключам (-k / BITBUCKET_PROJECT_KEYS), нужен BASE_URL:
BITBUCKET_BASE_URL=https://bitbucket.example.ru

# Аутентификация к Bitbucket REST: BASIC или TOKEN
BITBUCKET_AUTH_TYPE=TOKEN 

# Логин BitBucket (обязательно для обоих типов аутентификации)
BITBUCKET_USERNAME=bb_user      

# Для BASIC аутентификации:
#BITBUCKET_PASSWORD=bb_password_or_pat

# Для TOKEN аутентификации:
BITBUCKET_TOKEN=your_bitbucket_pat 

# BitBucket TLS
BITBUCKET_VERIFY_TLS=true # Проверять TLS-сертификат сервера BitBucket
# BITBUCKET_CA_CERT=/path/to/cacert.pem   # если используется кастомный сертификат

# Указывайте проект(ы) одним из способов:
# 1) Один URL:
#BITBUCKET_PROJECT_URL=https://bitbucket.example.ru/projects/PROJECT1
# 2) Несколько URL (через запятую или с новой строки):
#BITBUCKET_PROJECT_URLS=https://bitbucket.example.ru/projects/PROJECT1, https://bitbucket.example.ru/projects/PROJECT2
# 3) По ключам (требуется BITBUCKET_BASE_URL):
#BITBUCKET_PROJECT_KEYS=PROJECT1, PROJECT2

# ====== Git-креды для clone из Bitbucket (НЕ ОБЯЗАТЕЛЬНО) ======
# Нужны только если git-креды отличаются от REST-кредов.
# По умолчанию берутся: username = BITBUCKET_USERNAME, password = (PASSWORD при BASIC) или (TOKEN при TOKEN)
#BITBUCKET_GIT_USERNAME=bb_user_for_git
#BITBUCKET_GIT_PASSWORD=bb_password_or_pat_for_git

# ====== GitFlic REST API (обязательно) ======
GITFLIC_API_BASE_URL=http://localhost:8080/rest-api
GITFLIC_API_TOKEN=your_gf_api_token

# ====== GitFlic Push (по умолчанию HTTPS) ======
# Для HTTPS:
GITFLIC_GIT_USERNAME=your_gf_login
GITFLIC_GIT_PASSWORD=your_gf_transport_token
# Для SSH установите USE_SSH=true и сконфигурируйте SSH ключи в системе.
USE_SSH=false

# === Владелец GitFlic (единый для всех проектов) ===
# Тип обязателен (TEAM или COMPANY):
GITFLIC_OWNER_ALIAS_TYPE=TEAM  

# === Опции ===
# Видимость создаваемых проектов. true - private, false - public.
VISIBILITY_PRIVATE=true
# Язык программирования для проектов. Если не задан, будет выбран "Markdown".
LANGUAGE_DEFAULT=
# Сухой прогон (без создания проектов в GitFlic и без git push).
DRY_RUN=false
# Число одновременных потоков (параллельных миграций).
CONCURRENCY=3
# Рабочая директория для временных клонов репозиториев.
WORKDIR=/tmp/migrate-bb-to-gf
# Сохранение клонов репозиториев после завершения (для отладки).
KEEP_CLONES=false
